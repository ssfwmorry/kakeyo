# 新デザイン適用計画（デザイン原典への再整合）

- デザイン原典: https://claude.ai/artifact/2FanNbHvamWFqdHGu5kjkH （「かけよ デザイン再検討」・Design キャンバス）
- 作業ブランチ: `feat/nextjs-new-design`
- 実装先: `nextjs/src/v2/` と `nextjs/src/app/v2/`（ルート昇格・ファイル移動は全タスク完了後に別途行う）
- 本書の位置づけ: **デザイン原典を唯一の正**とし、現状の v2 実装との差分をタスクに分けたもの。
  各タスクは `tasks/` の 1 ファイルが仕様の全てを持ち、**別セッションで単独に実行できる**。

## デザイン原典の読み方（各セッション共通）

- 各画面は `project/<Name>.dc.html` というファイルで、`Artifact` ツールの `read` に
  `url` と `path: "project/<Name>.dc.html"` を渡すとローカルに保存され本文も返る。
  複数なら `paths` でまとめて取れる。**最初に `list`（`scope: "files"`）を 1 回呼ぶ**。
- `.dc.html` は `<x-dc>` のマークアップ（インライン style が寸法・色の正）と、
  `<script type="text/x-dc">` の `class Component extends DCLogic` の `renderVals()` に
  モックデータ・状態・操作（onClick / href）が入っている。**両方読む**。
- 数百バイトの小さいファイルは `<dc-import name="X" start="状態名" mode="個人|共有">` で
  「X をその状態で開いたもの」。状態名は X の script 内 `start` の分岐を探す。
- `Main.dc.html`（デザインの基礎）は色・文字・角丸・部品・画面遷移ルールの正。
  要点は [共通仕様.md](共通仕様.md) に写してある。
- モックの色値（`#A8520A` など）やカテゴリ名は見本。実装は DB のマスタと
  `tokens.css` の `--cat-*` を使う（Main「カテゴリ色の扱い：案A」）。

## 方針の決定事項（デザインが未定義・矛盾する点の解釈）

タスク着手時に迷わないよう、ここで確定する。変更したい場合は本書を直してからタスクを進める。

| # | 決定 |
|---|---|
| D1 | 中央の＋は「入力」を**全画面モーダル**で開く（上端 56px を残した固定高シート、タブバーを覆う）。**どのタブからでも開き、閉じると元の画面に戻る**（現状の「カレンダーへ遷移してシート」は廃止）。 |
| D2 | 並べ替えは**ドラッグ**（`@dnd-kit/core` + `@dnd-kit/sortable` を導入）。サーバは任意順を受ける `reorder*` Action を新設し、既存の swap（下と入れ替え）は v2 から外す。 |
| D3 | カテゴリ・方法・口座・予定カテゴリの色候補は **DB の 19 色のまま**（`--cat-*`）。デザインの 12 色パレットは使わない。追加時の初期選択は先頭の色。 |
| D4 | 削除の確認は**中央 290px のアラート**（Main の「Dialog は全てシート」の例外として原典が描いているとおり）。文言は「この◯◯を削除しますか？」＋「「{名前}」を削除します。削除すると元に戻せません。」＋「やめる｜削除」。 |
| D5 | トーストは `Toast.dc.html` の仕様（上端・黒地・完了は約 3 秒・エラーは自動で消えない・同時 1 枚）。 |
| D6 | ペア未設定のユーザには「個人｜共有」を出さない（デザイン未定義。旧実装と同じ）。 |
| D7 | 「いつもの」（ショートカット記録）はデザインに無いので **v2 から撤去**する。DB と旧画面は触らない。 |
| D8 | 任意日の選択 UI はデザイン未定義。既存の `react-day-picker` を v2 トークンで塗り、該当行の直下に展開する形を続ける。 |
| D9 | 定期の記録の削除は、文言「これまでに記録された分は残ります」に従い、**`records.planned_record_id` を NULL にしてから削除**する。 |
| D10 | デザインの無い画面・導線は**保留**（押せなくする）: 集計の「推移」「精算」（セグメントは disabled）、集計の行タップ明細（非リンク）、カレンダーの「すべての記録」（出さない）、設定の「アカウント」（出さない）。 |
| D11 | リマインダーはデザインに共有の変種が無いが、データはモードに連動させたまま（バッジもモードに従う）。 |
| D12 | お知らせ（期日超過）は `date < today`、リマインダー一覧（これから）は `date >= today`。当日分は一覧側。 |
| D13 | カレンダーの「今日の太字」「月外の日の薄表示」はデザインに無いので出さない。 |
| D14 | 空状態の文言はデザイン未定義。現行文言を残す（各タスクに明記）。 |
| D15 | 日別リストの記録行のタイルは、カテゴリアイコンが DB に無いため「36px の淡色タイル＋12px の色ドット」（Main のダーク見本と同形）。 |
| D16 | リマインダーの「予定に残す」で予定が作られるのは実装どおり「〜か月後」型のみ。文言「この日の予定はカレンダーに残ります」もその条件でだけ出す。 |
| D17 | モックにある影のうち、タブバー・中央＋・セグメント選択面・スイッチのつまみ・トーストは**そのまま影を付ける**。それ以外（数字キー等）は影なし。 |
| D18 | ボトムシートの UI は既存 v2 実装（「キャンセル｜見出し｜保存」のバー・末尾の削除ボタン）を引き継がず、原典のシート（×｜タイトル｜ゴミ箱、主ボタン、中央の確認）を正に作る。旧形は全廃。 |
| D19 | 「名前＋色」だけを持つマスタ（方法・予定カテゴリ・口座。名前だけのサブカテゴリも）はデータの形が同じなので、追加・編集シートを**共通部品 `MasterSheet`（`src/v2/components/master-sheet.tsx`）**に揃える。構成は原典 SetBank のシート（名前＋文字数カウンタ・補足・36px の色・主ボタン・右上ゴミ箱・中央の確認）。紐づくデータがあって削除できないときは、方法・口座は「削除できません」のアラート（名前と色の変更はできます）、予定カテゴリは原典どおりトースト。 |

## タスク一覧（依存順・各タスク 1 セッション想定）

状態: `[ ]` 未着手 / `[~]` 着手中 / `[x]` 完了。完了時にここを更新する。

| 状態 | ID | タスク | 依存 |
|---|---|---|---|
| [x] | [T01](tasks/T01-基盤部品.md) | 基盤部品の作り直し（トークン追加・シートのクローム・確認アラート・スイッチ・セグメント・リスト部品・日付書式・アイコン） | なし |
| [x] | [T02](tasks/T02-トースト.md) | トースト（Toast.dc.html） | T01 |
| [x] | [T03](tasks/T03-シェルとお知らせ.md) | アプリシェル（浮くタブバー・中央＋・ベル）とお知らせシート | T01, T02 |
| [x] | [T04](tasks/T04-カレンダーと予定.md) | カレンダー（ホーム）・TODO 追加シート・予定シート | T01, T03 |
| [x] | [T05](tasks/T05-入力フロー.md) | 入力フロー（全画面モーダル・カテゴリ→金額と詳細・共有・収入・前回の方法） | T01, T03 |
| [x] | [T06](tasks/T06-記録の編集.md) | 記録の編集（削除の確認・定期由来） | T05 |
| [x] | [T07](tasks/T07-ドラッグ並べ替え.md) | ドラッグ並べ替え基盤（dnd-kit・SortableList・reorder Action ×4） | T01 |
| [ ] | [T08](tasks/T08-設定トップとマスタ.md) | 設定トップ・カテゴリ・方法・予定カテゴリ | T01, T02, T07 |
| [ ] | [T09](tasks/T09-口座.md) | 口座タブ（推移・先月比・残高登録シート）と設定›口座（追加・編集・削除） | T01, T02 |
| [ ] | [T10](tasks/T10-定期の記録.md) | 設定›定期の記録（一覧・追加・編集・削除・並べ替え・共有） | T01, T02, T05, T07 |
| [ ] | [T11](tasks/T11-リマインダー.md) | 設定›リマインダー（一覧・追加・詳細・削除） | T01, T02, T03 |
| [ ] | [T12](tasks/T12-集計.md) | 集計（内訳のみ。推移・精算は保留） | T01, T03 |

## 各タスク共通の進め方

1. `docs/new-design/README.md`（本書）と `共通仕様.md`、担当タスクの `tasks/Txx-*.md` を読む。
2. 担当タスクに列挙されたデザインファイルを `Artifact` の `read` で取り直し、マークアップと script を読む
   （タスク仕様は要約なので、寸法・文言に迷ったら原典を優先する）。
3. `nextjs/AGENTS.md` の横断規約（scope / セッション / デモ注入 / Result 型 / labels / フォーム標準 / コメント）を守る。
4. 実装は `src/v2/**` と `src/app/v2/**` に閉じる。`src/features/**` の server 層（services / repositories / schemas）は再利用し、
   足りないものだけ追加する。旧 UI（`src/app/(private)`、`src/features/*/components`）は触らない。
5. `pnpm check:full` と `pnpm test` を通す。画面確認はデモログインで（`AGENTS.md`「画面の動作確認」）。
6. 完了したら本書のタスク一覧の状態を更新し、タスクファイル末尾の「完了メモ」に決めたこと・残したことを書く。
7. コミットはタスク単位（数コミット可）。コミットメッセージにタスク ID は書かない。

## 移行中の既知の段差（タスク完了で解消されるもの）

- 定期の記録の追加・編集が旧 `/note` へ送られ、保存後に旧 `/setting` に着地する → T10 で解消。
- リマインダーの追加が旧 `/setting` へ送られる → T11 で解消。
- 設定›口座が無く、口座の増減が旧 `/setting` からしかできない → T09 で解消。
- 集計タブが 404 → T12 で解消。
- Service Worker の開発中キャッシュ問題（`features/pwa/components/sw-register.tsx` で回避済み）。
- 再検証が旧パスと v2 パスの二重打ちになっている（ルート昇格時に整理）。
