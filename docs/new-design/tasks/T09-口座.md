# T09 口座タブと設定›口座

- 依存: T01, T02。
- 原典: `project/Bank.dc.html`（`open` = 残高登録シート）、`project/SetBank.dc.html`（状態: 一覧／追加／編集／削除の確認／削除できない。`SetBankAdd/Edit/Delete/Blocked` は dc-import）。

## デザイン仕様

### 口座タブ（Bank）— 個人専用。個人｜共有は出さない
- ヘッダー行 h44: 左ベル（T03 `NotificationBell`）／右 テーマ切替のみ。
- タイトル行: h1「口座」+ 右端ボタン **「＋ 残高を登録」**（h36 r18 `--secondary` 地・`--primary` 14/600）→ 残高登録シート。
- 総資産カード（白 r16 p16 gap 10）:
  - 「総資産（9/25 時点）」13px `--muted-foreground`（最新記録日 `M/D`）。
  - 金額行: 「1,284,300」30/700 + 「円」15/600 + 右端 **「先月比 +32,100」** 13/600 `--primary`（今月末時点 vs 先月末時点の総資産差。出せなければ非表示）。
  - 折れ線 SVG 326×96: 横罫線 3 本（`--line-soft`、y 31.5/63.5/95.5）、polyline `--primary` 2.5 round、終点に r4 の丸。ツールチップ・Y 軸無し。
  - X 軸ラベル「4月 5月 … 9月」11px `--muted-foreground` space-between。**直近 6 か月・各月末時点の総資産 1 点**（その月に記録が無ければ前月の値を引き継ぐ。全て無ければグラフを出さない）。
- 見出し行「口座ごとの残高」13px + 右リンク **「口座を編集 ›」** 14/600 `--primary` → `/v2/setting/bank`。
- 口座リストカード（白 r16）: 行 h52 px14 gap 12: 色丸 10px ／ 口座名 15 ／ 残高「823,000」15/600（「円」無し）。区切り `margin-left 36`。
- 空: 現行文言を残す。

### 残高登録シート
- 内容高シート、`aria-label="残高を登録"`、gap 12。ヘッダー: × ｜「残高を登録」｜ 空。
- カード 1（白 r14）: 行 h48「記録日」15 + 右チップ「2026/9/25」（h32 `--background` 地 r8）。今日固定。
- カード 2（白 r14）: 口座ごとの `label` 行 h52: 色丸 10 ／ 口座名 15 ／ `input inputmode=numeric` 幅 120 右寄せ 17/600、**placeholder = その口座の最新残高**（桁区切り。無ければ「0」）／「円」14px `--muted-foreground`。区切り `margin-left 36`。
- **「＋ 口座の行を追加」** テキストボタン（左寄せ h40 15/600 `--primary`）→ `/v2/setting/bank?add=1`（追加シートを開いた状態）。
- 主ボタン「登録する」（`mt-auto`）。空欄行は送らない。`postBankBalancesAction` を再利用。成功で閉じ「登録しました」。

### 設定›口座（SetBank）— 個人専用。共有モードでは設定トップの行を出さない
- ScreenHeader: 「‹ 設定」／空／空。ScreenTitle「口座」+ バッジ「個人の設定」。
- 説明「口座タブで残高を記録する口座です」。
- 白カード r14: 行 = button h52（`aria-label="{name}を編集"`）: 32px 色丸に頭文字 ／ 名前 16 ellipsis ／ シェブロン。並べ替え・編集モード無し。空: 「まだ口座はありません」（カード内 h88）。
- 追加行「口座を追加」→ 追加シート。
- 注記「口座は追加した順に並びます。色は口座タブの一覧と残高の登録で使われます。」
- シート: **T01 の `MasterSheet`**（README D19）で `entity='口座'` `nameLabel='口座名'` `maxLength={30}`。タイトル「口座を追加」／「口座を編集」、右上ゴミ箱は編集時（`aria-label="この口座を削除"`）。
  - 口座名: 白 h52 r12: ラベル「口座名」13 + `input maxlength=30 placeholder="例：楽天銀行"` 17/600 + 右カウンタ「{n}/30」。
  - helper 12px: 編集「名前や色を変えると、これまでの残高の記録にも反映されます」／追加「追加すると、残高の登録に行が増えます」。
  - 「色」+ ColorGrid 36px チェック無し（DB 19 色、初期は先頭）。
  - 主ボタン「追加する」／「保存する」、空なら「口座名を入れると追加できます」／「口座名を入れると保存できます」。
- 削除の確認（`aria-label="口座を削除"`）: 「この口座を削除しますか？」／「「{name}」を削除します。削除すると元に戻せません。」／やめる｜削除。
- 削除できない（残高の記録がある口座。`aria-label="口座を削除できません"`）: 「この口座は削除できません」／「「{name}」には残高の記録があります。名前と色の変更はできます。」／1 列「OK」（シートは開いたまま）。

## 現状
- `src/app/v2/(tabs)/bank/page.tsx`、`src/v2/features/bank/components/{bank-screen,balance-sheet,total-trend-chart}.tsx`。グラフは recharts で全記録点・ツールチップ、「前回比」、ベル無し、「口座を編集」無し、シートは白地・「キャンセル」、placeholder "0"。設定›口座は無い（設定トップの口座行は `/v2/bank`）。

## やること
1. `v2/features/bank/domain/month-ends.ts`（テスト付き）: `tableRows`（記録日ごと・引き継ぎ済み）から直近 6 か月の月末総資産と先月比を出す純関数。グラフは自前 SVG（recharts を外す）。
2. `bank-screen.tsx` のヘッダー・カード・見出し行・リストを上記に。
3. `balance-sheet.tsx` を共通クロームに、記録日 `YYYY/M/D`、placeholder、「＋ 口座の行を追加」。
4. `src/app/v2/(tabs)/setting/bank/page.tsx` + `v2/features/bank/components/{bank-setting-screen,bank-sheet}.tsx` 新設。`?add=1` で追加シートを開く。
5. サーバ: `getBankList` の戻りに `hasBalance: boolean`（`_count.bankBalances > 0`）を追加（`features/bank/server`、デモ射影も）。`upsertBankAction` / `deleteBankAction` を再利用（削除は `hasBalance` で事前に「削除できない」を出し、Action の `foreignKey` はフォールバック）。`revalidateBank()` に `/v2/setting/bank` を追加。
6. 設定トップの口座行を `/v2/setting/bank` に（T08 と重なるので、先に終わった側が直す）。

## 完了条件
- 口座タブで 6 か月の推移と先月比、残高登録（placeholder が最新残高）。設定›口座で追加・編集・削除、残高ありの口座は削除できない案内。

## 完了メモ（2026-09-26）
- 口座タブ `v2/features/bank/components/bank-screen.tsx`: ヘッダー左にベル（page から `headerLeft` で `NotificationBell` を渡す）、総資産カード（「M/D 時点」・先月比・自前 SVG の折れ線 326×96・月ラベル 6 つ）、見出し行「口座ごとの残高」＋「口座を編集 ›」→ `/v2/setting/bank`、口座リストの区切りは色丸の右から。recharts は v2 から外した（旧画面は使い続けるので依存は残す）。
- 推移は `v2/features/bank/domain/month-ends.ts` の `buildMonthEndTrend(rows, today)`（テスト付き）: 直近 6 か月の各月末時点 = その日以前で最後に記録された総資産。最初の記録より前の月は点を打たない。先月比 = 今月末時点 − 先月末時点で、先月末時点が無ければ出さない。
- 残高登録シート `balance-sheet.tsx`: 記録日 `YYYY/M/D`、placeholder はその口座の最新残高（無ければ 0）、「＋ 口座の行を追加」→ `/v2/setting/bank?add=1`、主ボタンは 1 口座でも打つまで「残高を入れると登録できます」。サーバ検証の文言は `useSubmissionErrorToast` でトーストに。
- 設定›口座 `src/app/v2/(tabs)/setting/bank/page.tsx` + `bank-setting-screen.tsx` + `bank-sheet.tsx`（`MasterSheet` で `entity='口座'` `nameLabel='口座名'` `maxLength=30`）。`?add=1` で追加シートを開いた状態で始め、閉じたら `router.replace` でクエリを外す。設定トップの口座行は `/v2/setting/bank` へ。
- サーバ: `getBankList` の戻り（`BankItem`）に `hasBalance`（`_count.bankBalances > 0`。デモ射影は `bankBalanceRows` の有無）。`revalidateBank()` は既に `/v2/setting` を layout 単位で再検証しており `/v2/setting/bank` を含むので追加していない。
- 削除できない案内: `useDeleteFlow` に `isKnownBlocked`、`MasterSheet` に `isDeleteBlocked` を追加。残高のある口座は確認の後、Action を呼ばずに「この口座は削除できません」のアラートを出す（デモは削除が no-op 成功になるため、サーバの `foreignKey` 分類だけに頼ると案内が出ない）。残高の無い口座は従来どおり Action → `foreignKey` をフォールバックにする。
- 確認: デモで口座タブ（6 か月の推移・先月比・口座を編集）、残高登録（placeholder が最新残高・登録で「登録しました」→ 閉じる）、設定›口座の一覧・編集シート・ゴミ箱 → 確認 → 「削除できません」（シートは開いたまま）、`?add=1` で追加シート → 追加で「登録しました」→ URL からクエリが外れる、ダークモード。`pnpm check:full` / `pnpm test` 通過。
- 残したもの: 先月比が 0 のときも「先月比 0」と出す（デモは 9 月の記録が無いためこの状態）。残高の無い口座の実削除（実データ）は未確認。口座タブの空状態の文言は現行のまま（D14）。
