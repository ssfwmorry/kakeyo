# T06 記録の編集（削除の確認・定期由来）

- 依存: T05。
- 原典: `project/RecordEdit.dc.html`（`confirm` = 削除の確認、`fromPlanned` = 定期由来）、`project/RecordEditDelete.dc.html`、`project/RecordEditPlanned.dc.html`。

## デザイン仕様
- 背面はカレンダー + 暗幕 `rgba(12,16,17,.42)`（`--overlay` で可）。シートは入力②と同じ**全高固定**（`top 56`、`--background`、gap 14）。入口は日別リストの記録行。
- ヘッダー h48 grid `44px 1fr 44px`: 左 × 36px 丸（`aria-label="閉じる"`）／中央 カテゴリピル（`aria-label="カテゴリを変える"`、押すと入力①へ。①では個人｜共有はロック表示）／右 **ゴミ箱** 36px 丸（`--muted` 地・`--destructive` アイコン、`aria-label="この記録を削除"`）。**個人｜共有セグメントは無い**（共有の記録はピルの「共有」タグで示す）。
- 日付＋メモカード: 入力②と同じ。前の日ボタンも上限で `--icon-muted`。
- **定期由来バナー**（`plannedRecordId` があるとき、カードの直下）: `margin-top -8`、`padding 8 12`、r10、`--secondary` 地、Repeat 16 `--primary`、本文 12px lh1.5 `--foreground` **「定期の記録から作られた記録です。日付は同じ月の中でだけ変えられます。」**。このとき前の日は元の記録の月の 1 日まで、次の日は今日まで。暦も同月に制限。
- 方法ラベルは **「支払方法」／「受取方法」**（「前回の…」は付けない）。ピルは入力②と同じ。
- 金額行・テンキーは入力②と同じ。
- 送信バー: **「保存する」**／不可時「金額を入れると保存できます」（金額 0）。共有記録のメモ必須は維持し「メモを入れると保存できます」（優先はメモ→金額）。
- 削除の確認（ConfirmAlert）: 「この記録を削除しますか？」／「{M月D日（曜）}の「{カテゴリ › サブ} {−|+}{金額}円」を削除します。削除すると元に戻せません。」／やめる｜削除。削除成功でシートを閉じ「削除しました」。

## 現状（T05 後）
- 日別リストの行 → `toRecordDefault` → `NoteModalProvider.open({ editing })` で全高モーダルが開き、②から始まる。ヘッダーは左が×（閉じる）・中央がピル（①へ）・右がゴミ箱で、押すと『この記録を削除しますか？』の `ConfirmAlert`（**本文が無い**）。定期由来の区別なし（保存時にサーバの `sameMonthOnly` がトーストで返るだけ）。

## やること
1. `NoteRecordDefault`（`v2/features/note/components/note-state.ts` 付近）に `plannedRecordId: Id | null` を足し、`toRecordDefault` で `RecordListItem.plannedRecordId` を通す。
2. ~~編集用ヘッダー（× ／ ピル ／ ゴミ箱）と送信バー左の削除撤去。`ConfirmAlert` に差し替え~~ → T05 で対応済み。残りは**確認の本文**を上記の書式で組むこと（`formatMonthDayWeekJa`、`formatSignedPrice`）。
3. 定期由来バナーと日付クランプ（前の日・次の日・暦）。
4. 方法ラベル、不可文言。
5. ~~編集の `isPair` は対象の区分に従う~~ → T05 で対応済み（`v2/features/note/actions.ts` の `resolveIsPair`）。
6. ~~編集は T05 の `NoteModalProvider` の `open({ editing })` で開く~~ → T05 で対応済み（日別リストの行から `open({ editing })`）。

## 完了条件
- 日別リストの記録行から編集が全高で開き、ゴミ箱→確認→削除でトースト。定期由来の記録でバナーが出て同月外に動かせない。

## 完了メモ（2026-09-26）
- `NoteRecordDefault` に `plannedRecordId` を追加（リポジトリ `findRecordForEdit`・デモ射影・`toRecordDefault` を通す。テスト更新）。
- 日付の可動範囲は `v2/features/note/domain/editable-dates.ts` の `editableDateRange`（テスト付き）: 定期由来は元の記録の月の 1 日〜「月末か今日の早い方」、それ以外は今日まで。`lib/shared/domain/date.ts` に `firstDayOfMonthJst` / `lastDayOfMonthJst` を追加（テスト付き）。`amount-step.tsx` の `DateRow` はこの範囲で前後ボタンを無効にし、暦は端の外を `disabled`、下限があるときは `startMonth` / `endMonth` で月送りも止める。
- 定期由来バナー（`PlannedBanner`。カード直下・`-mt-2`・`--secondary` 地・Repeat 16px）、方法ラベルは編集時「支払方法／受取方法」のみ、新規は「支払方法／受取方法」で候補の先頭を使う。送信バーの不可文言はメモ → 金額の順（「金額を入れると保存／登録できます」を追加。新規にも効く）。
- 削除確認の本文は `deleteDescription`（画面に見えている日付・カテゴリ › サブ・符号付き金額。原典の confirm と同じ）。
- 確認: デモ（ペアあり）で 9/1 の「住居 家賃」（定期由来）を開くとバナーが出て前の日が無効、暦は 9 月で月送り不可・27 日以降は選べない。確認本文「9月1日（火）の「住居 −80,000円」を削除します。…」。クリアで「金額を入れると保存できます」。定期由来でない記録はバナー無し・前後とも押せる。削除 → シートが閉じて「削除しました」。新規は「金額を入れると登録できます」「支払方法」。`pnpm check:full` / `pnpm test` 通過。
- 残したもの: 確認本文の値は保存済みの値ではなく編集中の値（金額や日付を触った直後に削除すると変えた値が読み上げられる。原典と同じ挙動なので据え置き）。
