# T10 設定›定期の記録（一覧・追加・編集・削除・並べ替え・共有）

- 依存: T01, T02, T05（テンキー・カテゴリ展開パネルの部品を流用）, T07。
- 原典: `project/SetPlanned.dc.html`（script の `start`: 一覧／追加／追加の詳細／編集／削除の確認／並べ替え、`mode`: 個人／共有。`SetPlannedAdd/AddDetail/Edit/Delete/Pair/PairEdit` は dc-import）。

## デザイン仕様

### 一覧
- ScreenHeader: 「‹ 設定」／空／**「並べ替え」⇄「完了」**。ScreenTitle「定期の記録」+ バッジ（モードに従う）。
- 説明 **「毎月決まった日に、自動で記録されます」**。
- サマリー 2 枚（grid 2 列 gap 8、白 r14 `padding 12 14`）: **「毎月の収入」** `+{合計}` 18/700 `--primary` ／ **「毎月の支出」** `−{合計}` 18/700 `--foreground`。合計は一覧の isPay 別の単純和（共有では立替・共有のお金を問わず全行）。
- 白カード r14。行 h64 px14 gap 12:
  - 先頭 w40 縦積み: **「毎月」** 10px `--muted-foreground` + `{D}日` 17/700。
  - 本文（先頭列の右にだけ上線）: 8px 色丸 + `{カテゴリ} › {サブ}`（サブ無しはカテゴリのみ）15 nowrap ／ 補足 12px `--muted-foreground` ellipsis: `[方法, (共有かつ支出なら 立替 ? 「自分が立替」 : 「共有のお金」), メモ]` を「 · 」で連結。**パートナーの立替行**（`isPair && pairUserName !== null && !isSelf`）: `{pairUserName}さんの立替 · {メモ}`。
  - 金額 16/600: 支出 `−{金額}` `--foreground` ／ 収入 `+{金額}` `--primary`。
  - 末尾: 通常シェブロン、並べ替え中はハンドル。編集可能行は button（`aria-label="毎月{D}日 {カテゴリ › サブ} を編集"`）→ 詳細シート（編集）。**パートナーの立替行は押せない div** で末尾に南京錠（`aria-label="パートナーのみ編集できます"`）。並べ替え中は行タップ無効。
  - 空: 「まだ定期の記録はありません」（カード内 h88）。
- 追加行「定期の記録を追加」（並べ替え中は非表示）→ カテゴリシート。
- 注記: 並べ替え中「ドラッグして並べ替えます。並び順は一覧の表示にだけ使われます。」／共有「パートナーが立て替える定期の記録は、パートナーだけが編集できます。共有／個人は登録後に切り替えられません。」／個人「行をタップすると編集・削除できます。」
- 行の並びは `sort` 順（日付でソートしない）。

### 追加 step1: カテゴリシート
- **全高固定**シート（内側スクロール可）、`padding 8 16 34`、gap 12。
- ヘッダー: 左 新規は ×「閉じる」／詳細から来たときは ‹「詳細に戻る」。中央 **「定期の記録を追加」**／詳細から来たときは **「カテゴリを変える」**。右 空。
- 「支出｜収入」セグメント `lg`。切替で展開・方法をクリア。
- ラベル「カテゴリ」+ 4 列グリッドと展開パネル（**T05 の入力①と同じ部品**を使う。「なしで進む」含む）。チップで詳細へ。サブカテゴリなしは即詳細へ。

### 追加 step2 / 編集: 詳細シート
- 全高固定、`padding 8 16 0`、gap 14、テンキーを下に張り付け。
- ヘッダー h48 grid `44px 1fr 44px`: 左 新規 ‹「カテゴリに戻る」／編集 ×「閉じる」。中央 白ピル（`aria-label="カテゴリを変える"`、10px 色丸 + `{カテゴリ › サブ}` 15/600 + 種別チップ「支出」（`--muted` 地）／「収入」（`--secondary`/`--primary`）+ 共有なら「共有」チップ。押すとカテゴリシート（`fromDetail`）。右 編集のみゴミ箱（`aria-label="この定期の記録を削除"`）。
- 白カード r14: 行 1 h52: Repeat アイコン + ラベル **「毎月」**（w34）+ セグメント（`role=group aria-label="毎月の記録日"`、`--background` 地）**「1日」「10日」「15日」「25日」**（`getDayClassifications` の value）。行 2 h52 `border-top --line-soft`: メモアイコン + 「メモ」+ `input aria-label="メモ" placeholder="任意（例：動画サブスク）"`。カード下 12px `--muted-foreground`: `次は {M}月{D}日 に自動で記録されます`（選んだ日が今日より後なら今月、そうでなければ翌月）。
- 共有かつ支出のみ: ラベル **「だれのお金で払う？」** + セグメント `xl`「自分が立替／あとで精算する」｜「共有のお金／精算しない」。切替で方法を先頭にリセット。
- ラベル 支出「支払方法」／収入「受取方法」+ 方法ピル（T05 と同じ）。候補が変わったとき選択が候補に無ければ先頭。
- 金額行（`mt-auto` h56）: 「クリア」+ `output aria-label="毎月の金額"` `−4,800`（支出 `--foreground`／収入 `--primary`）+「円」。テンキー（T05 の `Keypad`）。上限は `priceSchema` に従う。
- 保存バー: 新規 **「登録する」**／編集 **「変更を保存」**。無効（金額 0 またはカテゴリ未選択）「金額を入れると登録できます」。**共有でもメモ必須では無い**。
- 保存: 新規は追加、編集は差し替え、閉じてトースト「登録しました」／「変更しました」。

### 削除の確認
- `ConfirmAlert`（`aria-label="定期の記録を削除"`）: 「この定期の記録を削除しますか？」／「{カテゴリ › サブ}（毎月{D}日）は来月から記録されなくなります。これまでに記録された分は残ります。」／やめる｜削除。削除でシートも閉じ「削除しました」。

## 現状
- `src/v2/features/planned-record/components/planned-record-screen.tsx`（Server Component、行は旧 `/note?plannedRecordId=` へリンク、追加は旧 `/note?planned=new`）。シート・削除・並べ替え・ロック行・脚注が無い。

## やること
1. `src/v2/features/planned-record/actions.ts`: `upsertPlannedRecordAction` / `deletePlannedRecordAction` の v2 版（遷移せず toast、`revalidatePath('/v2/setting','layout')`、編集時の `isPair` は対象自身から）。`plannedRecordUpsertSchema`（`features/planned-record/schemas.ts`）と `service.upsertPlannedRecord` を再利用。
2. **削除の意味を文言に合わせる（D9）**: `features/planned-record/server/repositories` の削除を「同一トランザクションで `records.planned_record_id` を NULL 更新 → 削除」に変更（scope 付き）。既存の `foreignKey` 分類は残す。
3. 画面を Client 化し、一覧・カテゴリシート・詳細シート・確認・並べ替え（`reorderPlannedRecordAction`、T07）を実装。page で `getTypeCardList` / `getMethodCardList` / `getDayClassifications` / `hasPair` を取る。
4. ロック行、補足テキスト、脚注 3 種、空文言、`次は…` ヒント。

## 完了条件
- デモ（ペアあり／なし）で追加（カテゴリ→詳細→登録）・編集・削除・並べ替えができ、共有ではパートナーの立替行が押せない。旧 `/note` への導線が無い。

## 完了メモ
（2026-09-26）
- 画面: `v2/features/planned-record/components/planned-record-screen.tsx` を Client 化。ヘッダー右「並べ替え」⇄「完了」、サマリー 2 枚、`SortableList` + 既存 `reorderPlannedRecordAction`（並べ替え中は行タップ無効・追加行非表示・脚注切替）、パートナーの立替行は `ListCellStatic` + 南京錠（`aria-label="パートナーのみ編集できます"`）。行の文言と編集初期値の導出は `domain/row-text.ts`（`itemTitle` / `itemDescription` / `isLockedItem` / `toPlannedRecordDefault`。テスト付き）。
- シート: `planned-record-sheet.tsx`。全高固定 1 枚の中でカテゴリ（`TypeGrid`）⇄ 詳細を切り替える。詳細ヘッダーは `44px 1fr 44px` の `SheetHeader` に `TypePill`、「毎月」の記録日は `Segment`（`tone='background'` を追加）、「次は M月D日 に自動で記録されます」は `domain/next-record-date.ts`（テスト付き。今日と同じ日は翌月）。保存は `v2/features/planned-record/actions.ts` の `savePlannedRecordAction`（編集の `isPair` は対象自身から、新規は `getEffectivePairMode`）、削除は `deletePlannedRecordAction` をゴミ箱 → `ConfirmAlert` から直接呼ぶ（成功でシートが閉じるので `showToast` を直に出す）。
- 入力フローから切り出した共有部品: `note/components/type-pill.tsx`（`TypePill` / `typeLabel`）、`method-pills.tsx`（`MethodPills` / `FieldLabel`）、`amount-row.tsx`（`AmountRow`。`label` で output の名前を変える）。`TypeGrid` を `type-step.tsx` から export し、選び直しのときに選択中の枠を出す `selectedId` を足した。
- D9: `deletePlannedRecordById` を `$transaction` に変え、scope 内の確認 → `records.planned_record_id` を NULL（scope 付き `updateMany`）→ 削除の順にした。`foreignKey` 分類は残している。`plannedRecordErrorMessage` を `features/planned-record/domain/error-message.ts` に出し、旧 Action と共有。
- デモ: `dataset/planned-records.ts` に相手の立替（`partnerInternet`、record_type=5）を足し、共有の一覧でロック行が見えるようにした。`dataset.test.ts` の planned_records の整合は `resolveRecordType` で立替を許す形に。
- 決めたこと: 新規の「毎月」の既定は `day_classifications` の先頭（1 日）。方法は候補に無ければ先頭（前回の方法の仕組みは定期には持ち込まない）。「並べ替え」は一覧が空でも出す（原典どおり）。
- 確認: デモ（ペアあり・個人）で 一覧・編集シート（ピル → 「カテゴリを変える」→ ‹ 詳細に戻る）・ゴミ箱 → 確認本文「住居（毎月1日）は来月から…」→ 削除でシートが閉じる、追加（食費 → 外食 → 15日・メモ・−1,000 → 登録する）で「登録しました」（DOM の監視で確認。トーストは 3 秒で消えるため目視は間に合わない）、並べ替えモード（ハンドル・脚注・追加行なし）。共有で 3 行（はなこさんの立替 · 光回線 が南京錠・押せない）、住居の編集で「だれのお金で払う？」を立替に切り替えると方法候補が個人のものに入れ替わり先頭が選ばれる。`pnpm check:full` / `pnpm test` / `pnpm build` 通過。
- 残したもの: ドラッグの実操作と実データでの削除（records の NULL 化）は未確認（デモは書き込みが no-op）。`/v2/summary` の prefetch 404 は T12。
