# T08 設定トップ・カテゴリ・方法・予定カテゴリ

- 依存: T01, T02, T07。
- 原典: `project/Setting.dc.html`、`project/SetType.dc.html`（`isPay` / `editing`）、`project/SetTypeEdit.dc.html`（`subOpen` / `confirm`）、`project/SetMethod.dc.html`（`tab` / `editing` / `open` / `isNew` / `confirm`）、`project/SetPlanType.dc.html`（状態: 一覧／追加／編集／削除の確認／保存したあと／削除できなかったとき／並べ替え。`SetPlanTypeAdd` 等は dc-import）。

## デザイン仕様

### 設定トップ（Setting）
- ヘッダー行: 左ベル（T03）／右 テーマ切替 + 個人｜共有。h1「設定」`margin 0 0 4px`。
- グループ 3 つ（各 縦 gap 6、下余白 14）: 見出し pl14 13px `--muted-foreground`、白カード r14。行 = リンク h48 px14 gap 12: 色タイル 30×30 r8 に白アイコン 17px stroke 2 ／ 本文（2 行目以降 `border-top`、線はタイルの右から）: ラベル 16 flex-grow ／ 件数 15px `--muted-foreground` ／ シェブロン 14 `--icon-muted`。
  - 家計管理: カテゴリ（Tag、`--primary`、件数）→ `/v2/setting/type` ／ 方法（CreditCard、`#2463B5`）→ `/v2/setting/method` ／ 口座（Landmark、`#197A4B`）→ `/v2/setting/bank`（T09。それまでは `/v2/bank`）／ 定期の記録（Repeat、`#6D4BC4`）→ `/v2/setting/planned-record`。
  - 予定管理: 予定カテゴリ（Calendar、`#C2610C`）→ `/v2/setting/plan-type` ／ リマインダー（Bell、`#B83A3A`）→ `/v2/setting/reminder`。
  - その他: とりせつ（Book、`--muted-foreground`、外部 Notion。右端はシェブロン）／ お問い合わせ（Mail、`/inquiry`）／ ログアウト（LogOut、タイル `--destructive`、**文字も `--destructive`**）。件数なし。「アカウント」行は出さない（D10）。
  - タイル色はデザインの直値を `--tile-*` としてトークン化（ダークは同値でよい）。
- 口座行は共有モードでは出さない（現状維持。Main「個人専用の画面では出さない」）。
- ログアウトの確認は `ConfirmAlert`（「ログアウトしますか？」／説明なし／やめる｜ログアウト）。
- 件数: 現行どおり一覧の `length`。リマインダーは全件で可。

### カテゴリ一覧（SetType）
- ScreenHeader: 「‹ 設定」／空／**「編集」⇄「完了」**。ScreenTitle「カテゴリ」+ バッジ（モードに従う）。
- セグメント `md`「支出」｜「収入」。
- 白カード r14。行 = リンク h52: 編集中は行頭に赤「−」バッジ 22px（`--destructive` 地・白 Minus 12 stroke 3、`aria-label="削除"`、押すと `ConfirmAlert`「このカテゴリを削除しますか？」→ `deleteTypeAction`）／ 色丸 32 頭文字 ／ 本文: 名前 16 + サブカテゴリ要約 12 `--muted-foreground`（縦 gap 1。規則: 0 件「サブカテゴリなし」、≤3 件は全列挙「ランチ、ディナー、カフェ」、4 件以上は先頭 2 件 + 「 ほかN件」）／ 右端: 編集中はハンドル、通常はシェブロン。編集中も行タップで編集画面へ行ける。
- 追加行「カテゴリを追加」（編集中も表示）→ `/v2/setting/type/new?isPay=`。
- 注記: **「「編集」で並べ替えと削除。並び順は入力画面のカテゴリの並びにそのまま使われます。」**
- 並べ替えは T07 の `SortableList` + `reorderTypeAction`。
- 空: 現行文言を残す。

### カテゴリを編集（SetTypeEdit）
- ScreenHeader: 「‹ カテゴリ」／中央 **「カテゴリを編集」**（新規は「カテゴリを追加」）／右 **「保存」** 16/700。
- 本文 `padding 8 16 34` gap 18:
  1. 見出し行 gap 14: プレビュー丸 56px（**選択中の色と入力中の名前の頭文字に追従**）+ 名前フィールド（h48 px14 r12 白、ラベル「名前」13 + input 17/600、`aria-label="カテゴリ名"`、maxLength 10。placeholder は出さない）。
  2. 「色」+ ColorGrid 40px チェックあり。
  3. 「サブカテゴリ」（編集時のみ）: ヘッダー右に **「✎ で名前の変更・削除 / ≡ で並べ替え」** 12px。白カード r14: 行 h48 `padding 0 6 0 14`: 名前 16 ／ 鉛筆 44×44 `--primary` 16px（`aria-label="{名前}を編集"`）／ ハンドル枠 32×44。区切り `margin-left 14`。追加行 `<label>` h48 px14 gap 12: 緑丸 22px `#197A4B`（`--tile-green`）白＋ 12 stroke 3 + `<input placeholder="サブカテゴリを追加" aria-label="サブカテゴリを追加">`（Enter で `upsertSubTypeAction`）。並べ替えは `reorderSubTypeAction`。
  4. **「このカテゴリを削除」** ボタン h48 r14 白 `--destructive` 16/600 → `ConfirmAlert`「このカテゴリを削除しますか？」／「「{名前}」を削除します。削除すると元に戻せません。」。
- サブカテゴリ編集シート: 共通クローム。タイトル「サブカテゴリを編集」、右上ゴミ箱（`aria-label="このサブカテゴリを削除"`）→ `ConfirmAlert`「このサブカテゴリを削除しますか？」／「「{名前}」を削除します。削除すると元に戻せません。」。フィールド h48「名前」+ input（`aria-label="サブカテゴリ名"`）。注記「名前を変えると、これまでの記録にも新しい名前で表示されます」。主ボタン「保存する」。
- 保存成功で一覧へ戻りトースト。

### 方法（SetMethod）
- ScreenHeader: 「‹ 設定」／空／「編集」⇄「完了」。ScreenTitle「方法」+ バッジ。
- セグメント `md` **「支払」「受取」「精算」**（個人モードでは精算を出さない）。タブ注記 12px: 支払「支出を記録するときに選ぶ方法です」／受取「収入を記録するときに選ぶ方法です」／精算「集計の「精算」で、立替分をやり取りするときに選ぶ方法です」。
- 白カード r14。行 = button h52 → 編集シート（編集中もタップ可）。編集中: 行頭「−」（`aria-hidden`。押すと確認→削除）+ ハンドル。通常: シェブロン。丸 32 + 名前 16。
- 追加行「{支払方法|受取方法|精算方法}を追加」。
- 注記「「編集」で並べ替えと削除。並び順は入力画面・精算画面の候補の並びにそのまま使われます。個人モードでは「精算」は出ません。」
- シート: **T01 の `MasterSheet`**（README D19。口座と同形）。タイトル「{entity}を追加」／「{entity}を編集」、右上ゴミ箱は編集時（`aria-label="この{entity}を削除"`）。名前 h52 + カウンタ「{n}/10」（placeholder 支払「例：楽天カード」／受取「例：共有口座」／精算「例：現金手渡し」）。「色」+ ColorGrid 36px。主ボタン「追加する」／「保存する」（名前空で無効「名前を入れると追加できます」／「名前を入れると保存できます」）。
- 確認: 「この{entity}を削除しますか？」／「「{名前}」を削除します。削除すると元に戻せません。」／やめる｜削除。記録に使われていて消せないときは「この{entity}は削除できません」／「「{名前}」には記録があります。名前と色の変更はできます。」／OK（シートは開いたまま）。
- 並べ替え `reorderMethodAction`。

### 予定カテゴリ（SetPlanType）
- ScreenHeader: 「‹ 設定」／空／「編集」⇄「完了」（切替でシートとトーストを閉じる）。ScreenTitle「予定カテゴリ」+ バッジ。
- リード文 13px: **「予定を追加するときに選ぶカテゴリです。色はカレンダーの帯の色になります」**。
- 白カード r14。行 = button（`aria-label="{名前}を編集"`）h52、丸 32、名前 16 ellipsis。編集中 = ハンドル（**「−」は無い**）・行タップ無効・**追加行非表示**。通常 = シェブロン。
- 追加行「予定カテゴリを追加」。
- 注記: 通常「行をタップすると名前と色を変えられます。「編集」で並べ替え。」／編集中「ドラッグして並べ替えます。並び順は予定を追加するときの候補の並びになります。」
- シート: タイトル「予定カテゴリを追加」／「予定カテゴリを編集」、右上ゴミ箱は編集時（`aria-label="この予定カテゴリを削除"`）。フィールド **h52**「名前」+ input `maxlength=10` placeholder「例：通院」（`aria-label="予定カテゴリの名前"`）+ 右端カウンタ「{n}/10」。「色」+ ColorGrid 36px。主ボタン「追加する」／「保存する」、空なら「名前を入れると追加できます」／「名前を入れると保存できます」。
- 保存: 閉じてトースト「登録しました」／「変更しました」。
- 確認（`aria-label="予定カテゴリを削除"`）: 「この予定カテゴリを削除しますか？」／「「{名前}」を削除します。削除すると元に戻せません。」。
- 削除結果: 使用中 → 確認だけ閉じ**シートは開いたまま**、エラートースト **「「{名前}」を使っている予定があるので削除できません」**／未使用 → 閉じて「削除しました」。
- 並べ替え `reorderPlanTypeAction`。

## 現状
- `src/v2/features/setting/components/*`、`src/v2/features/type-method/components/*`、`src/v2/features/plan-reminder/components/plan-type-*`。シートは `SheetActionBar`（キャンセル｜見出し｜保存）+ 末尾 `SheetDeleteButton` + 旧 ConfirmDialog、ColorGrid 40px、並べ替えは SwapButton、注記・リード・カウンタ・無効文言が無い、ベルはリマインダーへのリンク、とりせつは外部アイコン。

## やること
1. 設定トップ: タイル色・アイコン 17px・シェブロン統一・ログアウト確認・口座行のリンク先。
2. カテゴリ一覧／編集: 上記。`SortableList`、「−」バッジ、要約規則、プレビュー追従、サブカテゴリ節、ヘッダー中央タイトル。
3. 方法: シートは T01 で `MasterSheet` 化済み。残りは「−」と `SortableList`。
4. 予定カテゴリ: リード・注記切替・追加行非表示・カウンタ・無効文言・削除不可トースト（`deletePlanTypeAction` の `foreignKey` 時に対象名入り文言。`features/plan-reminder/actions.ts` で `findPlanTypeInScope` の `name` を使って組むか、v2 側の薄い Action にする）。
5. `sheet-delete-button.tsx` / `SwapButton` の v2 内参照を撤去（`sheet-action-bar.tsx` は T01 で削除済み）。

## 完了条件
- デモ（ペアあり）で設定→カテゴリ／方法／予定カテゴリの追加・編集・削除・並べ替えがデザインどおり動き、トーストが T02 の見た目で出る。予定カテゴリの使用中削除で赤トーストとシート維持。

## 完了メモ（2026-09-26）
- 削除の流れ（確認 → 実行 → 成功で片付け／紐づきがあれば説明）を `v2/components/delete-flow.tsx` に共通化（`useDeleteFlow` / `DeleteAlerts` / `RemoveBadge`）。`MasterSheet` もこれに載せ替え、一覧の編集モードの − とカテゴリ編集画面の「このカテゴリを削除」が同じ部品を使う。`sheet-delete-button.tsx` は削除。
- `NameCell` を編集モード対応に拡張（行頭の − `onRemove`、編集中も名前を押して開ける `isOpenableWhileEditing`、`ariaLabel`）。編集中の行はボタン／リンクそのものにせず、名前の部分だけを押せる範囲にする。
- 設定トップ: タイル色を `--tile-blue/green/purple/orange/red`（`tokens.css`。ダークは同値）に、アイコン 17px、口座は Landmark、とりせつの右端もシェブロン。口座行の行き先は T09 まで `/v2/bank` のまま。
- カテゴリ一覧: 編集中に − と `SortableList`、行は編集中もリンク。サブカテゴリ要約は `domain/sub-type-summary.ts`（≤3 件は全列挙、4 件以上は先頭 2 件 + ほかN件。テスト付き）。
- カテゴリ編集: ヘッダー中央タイトル・右「保存」、プレビューの丸は入力中の名前と選択中の色に追従（`ColorGrid` の `onChange`）。サブカテゴリはハンドル常設の `SortableList` + `reorderSubTypeAction`、追加行の丸は `--tile-green`、区切りは先頭ブロックの右から。サブカテゴリのシートは `MasterSheet` にカウンタ無し・h48・`aria-label="サブカテゴリ名"`。
- 方法: − と `SortableList`、編集中も行タップでシート。− からの削除も「削除できません」アラート（`methodForeignKeyHandling` を `method-sheet.tsx` から共有）。
- 予定カテゴリ: リード・注記の切替・編集中は追加行非表示・行タップ無効・− 無し。「編集」⇄「完了」でシートとトーストを閉じる。シートの `aria-label="予定カテゴリの名前"`。
- `MasterSheet` に `nameAriaLabel` / `counter` / `nameHeight` を追加。`ScreenHeaderAction` は `type='submit'` を受けられる（カテゴリ編集の「保存」）。
- 確認: デモ（ペアなし）で 設定トップのタイル色 6 種、カテゴリ一覧の編集モード（− ×4・ハンドル ×4・行はリンクのまま・確認本文「「食費」を削除します。…」）、カテゴリ編集で名前を打つと丸の頭文字が変わる・色の選択で丸の色が変わる・サブカテゴリの鉛筆／ハンドル／追加欄・「このカテゴリを削除」の確認・サブカテゴリシート（カウンタ無し・注記・ゴミ箱→確認）、方法の編集モードで行タップからシート・− → 確認 → 削除で「削除しました」、予定カテゴリのリード／注記切替／追加行非表示／シートのカウンタと「名前を入れると保存できます」。`pnpm check:full` / `pnpm test` 通過。
- 残したもの: 行頭の − は原典の `aria-hidden` ではなく `aria-label="削除"` のボタン（押せる要素を隠すと支援技術から届かないため）。削除不可（実データ）の確認は方法・カテゴリとも未実施（デモは削除が no-op 成功）。共有モードの「精算」タブはペアなしデモでは出ないため未確認。
