# T12 集計（内訳のみ。推移・精算は保留）

- 依存: T01, T03。
- 原典: `project/Summary.dc.html`（内訳タブのみ設計。推移・精算はセグメントの選択肢だけ）。

## デザイン仕様
- ヘッダー行 h44: 左ベル（Calendar / Setting にはあるが Summary の原典ではボタン。T03 の `NotificationBell` を置いてよい）／右 テーマ切替 + 個人｜共有。
- h1「集計」。
- セグメント `md`「内訳」「推移」「精算」。**推移・精算は `disabled`**（D10。押せない見た目、選択不可）。ペア有無での出し分けはしない。
- 月ナビ行（gap 4）: 「前の月」「次の月」36px 丸（透明地）／「2026年9月」16/600 ／ 右端（gap 6）ピル h30 r999 13px: **「支出」**（選択 = `--foreground` 地・白・600）／**「収入」**（非選択 = `--card` 地・`--foreground`）。
- ドーナツカード（白 r16 `padding 18 0` 中央）: 180×180 SVG、`r=70 stroke-width=22`、`rotate(-90)`、各弧 `dasharray = 割合×439.82 − 2`（2px の隙間）、`dashoffset = −累積`。中央: 「支出合計」／「収入合計」12px `--muted-foreground` ／ 合計 22/700 ／ 「円」12px。凡例・ツールチップ無し。
- カテゴリリストカード（白 r16）: 行 h56 px14 gap 12: 10px 色丸 ／ 本文（縦 gap 6）: 名前 15 + 「32.0%」12px `--muted-foreground`（baseline）、その下に 4px 進捗バー（track `--line-soft`、fill = カテゴリ色、幅 = pct%）／ 金額 15/600 ／ シェブロン `--icon-muted`。区切り `border-top`。**行タップの明細はデザイン未設計なので非リンク**（D10。シェブロンは出す）。
  - `pct = round(value/total*1000)/10`、`pctText = pct.toFixed(1)+'%'`、金額降順。精算行（`id=-1`、色 yellow）はそのまま出す。
- 空: 「表示するデータがありません」（旧文言、D14）。
- デザインに無いもの（出さない）: カテゴリ／方法トグル、立替込み／自分のみトグル、サブカテゴリ行、凡例、年月ジャンプ、スワイプ月移動。

## 現状
- v2 に集計は無い（タブバーのリンク先が 404）。旧 `src/app/(private)/summary` と `features/summary`（`fetchPieAction`、`domain/period.ts` の `shiftMonth/monthLabel/currentYearMonth`）がある。

## やること
1. `src/app/v2/(tabs)/summary/page.tsx`（`requireAuth`、`getEffectivePairMode`、`hasPair`、初期 `yearMonth = currentYearMonth()`、初期データは Server 側で `getPieShowData` 相当を 1 回呼ぶ）。`lib/server/pair/pages-without-pair.ts` の `PAIR_SCOPED_PATHS` に `/v2/summary` を追加。
2. `src/v2/features/summary/components/summary-screen.tsx`（ヘッダー・セグメント・月ナビ・支出／収入ピル・ドーナツ・リスト）。月移動と支出／収入切替は `fetchPieAction({ isType: true, isPay, isPair, isIncludeInstead, yearMonth })` をクライアントから呼ぶ。`isIncludeInstead` は個人モード true・共有モード false（旧既定）。
3. ドーナツは自前 SVG（`v2/features/summary/components/donut.tsx`）。色は `colorVar(colorName)`。
4. `Segment` の `disabled` 選択肢（T01）で推移・精算を保留表示。

## 完了条件
- デモで集計タブが開き、月移動・支出／収入切替でドーナツとリストが変わる。推移・精算は押せない。

## 完了メモ（2026-09-26）
- 画面: `src/app/v2/(tabs)/summary/page.tsx` + `v2/features/summary/components/summary-screen.tsx`。今月・支出の内訳を Server で 1 度取り、月移動と支出／収入の切替は Client が既存 `fetchPieAction` で取り直す（取得中は前の内容を薄くして残す）。「個人｜共有」の切替はページ側の `key` で画面の state ごと作り直す（月と支出／収入は今月・支出に戻る）。
- ドーナツ: `components/donut.tsx`（自前 SVG 180×180・r70・stroke 22）。弧と割合の計算は `domain/breakdown.ts` の `buildBreakdown`（金額降順、`pct` は小数 1 桁、弧は隙間 2px を引いた長さと累積の負の offset。テスト付き）。合計 0 のときは `--line-soft` の輪だけを出し、一覧は「表示するデータがありません」（D14）。
- 一覧の行は非リンク（D10）でシェブロンだけ出す。精算行（id=-1・yellow）はそのまま並ぶ。個人モードは立替込みなので、個人と共有に同名のカテゴリがあると同じ名前の行が 2 つ出る（旧画面と同じ。原典に区別の印が無いので付けていない）。
- サーバ: `features/summary/server/services.ts` に `getTypePie`（`getTypeSummary` → `buildTypePie`）を足し、既存 `fetchPieAction` のカテゴリ別もこれを通すようにした。デモ射影は既存の `queries/summary.ts` で足りた。`PAIR_SCOPED_PATHS` に `/v2/summary` を追加。
- 確認: デモ（ペアあり）で 個人／支出（5 行・ドーナツ）、収入（給与 100%）、前の月（8 月の収入に精算 0.2% の細い弧）、ダークモード、共有に切替（9 月・支出に戻り 4 行）、10 月で空状態。推移・精算は disabled で押せない。`pnpm check:full` / `pnpm test` / `pnpm build` 通過。
- 残したもの: 推移・精算の中身（デザイン待ち）。行タップの明細（デザイン待ち）。
