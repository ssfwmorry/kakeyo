# T04 カレンダー（ホーム）・TODO 追加シート・予定シート

- 依存: T01, T03。
- 原典: `project/Calendar.dc.html`（script の `sheet/draft/shared` = TODO 追加シート、`lanes` = 帯の割当）、`project/PlanAdd.dc.html`、`project/PlanEdit.dc.html`（`confirm` = 削除の確認）。

## デザイン仕様

### 画面（上から、本文 `padding 47px 16px 0`、gap 12）
1. ヘッダー行 h44: 左ベル（T03 の `NotificationBell`）／右 テーマ切替 + 個人｜共有。
2. 月見出し行（gap 8、align center）: `<h1>` **「9月」** 30/700 ／ 年 **「2026」** 17px `--muted-foreground` `margin-top 6` ／ 収支（`margin-left 6`）: ラベル「収支」12px `--muted-foreground` + 値「+121,550」15/600 `--foreground`（**符号によらず文字色**）／ 右端に「前の月」「次の月」36px 丸（`--card` 地・`--foreground`・シェブロン 18px stroke 2.4）gap 8。
3. 月グリッド `margin 0 -16px`、`--card` 地、`border-top/bottom 1px var(--border)`:
   - 曜日行 h24、11/600、日 `--destructive`・土 `--saturday`・平日 `--muted-foreground`。
   - セル h72 `padding-top 3` gap 1 `border-top 1px var(--line-soft)`、縦積み中央。**月外セルは空**（日付も帯も描かない、D13）。
   - 日付 24px 丸 13px。選択日: `--primary` 地・白・700。日曜祝日 `--destructive`、土曜 `--saturday`、平日 `--foreground`。今日の強調は無し（D13）。
   - 収支行 h11 9px（値なしでも高さ確保）。`+`/`−` + 絶対値。収入超過は `--primary`、それ以外 `--muted-foreground`。
   - 帯は最大 2 段 gap 2、h14 lh12 `padding 0 3` 10/600 nowrap。左端（期間開始 or 週頭）は `margin-left 2` + 左角丸 3、右端は `margin-right 2` + 右角丸 3。文字は左端でのみ。予定: `color-mix(色 var(--band-mix), var(--card))` 地・文字は色。リマインダー: `--card` 地・`border 1px 色`・文字は色。3 段目以降がある日は最下段を「他N件」（`--fill-soft` 地・`--muted-foreground`・r3・`margin 0 2`）。段割当は既存 `domain/event-lanes.ts` のまま。
4. ボタン行 grid 2 列 gap 10: **「記録」**（h44 r12 `--primary` 白 15/600、Plus 18 stroke 2.4）→ 入力モーダル（T05 まで現状の `?note=new`）／**「予定」**（h44 r12 `--secondary` 地・`--primary`）→ 予定シート。
5. TODO（縦 gap 8）: 見出し行 gap 6「TODO」12/700 `--muted-foreground` `letter-spacing .04em` + 件数「5件」12px。チップ群 **`flex-wrap` gap 6**。チップ h32 `padding 0 2px 0 12px` r16 `--card` 13px gap 4。共有なら先頭に Users 14px `--primary`（`aria-label="共有"`）。本文 ellipsis。末尾に × 28px 丸（透明・`--icon-muted`・X 12 stroke 2.6・`aria-label="{text}を削除"`）→ 即削除（確認なし）。末尾に **「＋ 追加」** チップ（h32 px12 `border 1px dashed var(--dash)` r16 透明 13px `--muted-foreground`）→ TODO 追加シート。0 件のとき「ありません」は出さない。
6. 日別見出し行 `margin-top 4`: `<h2>` **「9月25日（金）」** 17/600（全角括弧）。右端の「すべての記録」は**出さない**（D10）。
7. 日別カード `--card` r16（区切り 1px `--border`）:
   - 予定行 h48 `padding 0 14` gap 12: 縦棒 4×24 r2（予定カテゴリ色）、名前 15、「予定」12 `--muted-foreground`、シェブロン 14 `--icon-muted`。区切りは `margin-left 14`。押すと予定シート（編集）。
   - リマインダー行 h48: Bell 16（stroke = 色）、名前 15、「リマインダー」12、シェブロン。押すとお知らせシート（T03）。
   - 記録行 h60: タイル 36×36 r10（カテゴリ色を `color-mix` で淡く）の中に 12px の色ドット（D15）、タイトル「食費 · スーパー」15、副「夕飯の材料 · クレカ」12 `--muted-foreground`、金額「−2,480」16/600、シェブロン無し。区切り `margin-left 62`。押すと記録の編集（T06。それまでは現状のシート）。
   - 空: 現行文言「この日の記録・予定はありません。」を残す（D14）。

### TODO 追加シート
- 内容高シート、`role=dialog aria-label="TODOを追加"`、gap 14。ヘッダー: × ｜「TODOを追加」｜ 空。
- 白カード r14: 入力行 h52 `padding 0 14` `<input aria-label="やること" placeholder="やることを入力">` 16px 枠なし ／ 区切り `--line-soft` ／ 共有行 h60 gap 12: Users 18 `--muted-foreground`、「ペアと共有する」15 + 「オンにすると、ペアのTODOにも表示されます」12 `--muted-foreground`、右にスイッチ（T01 `Switch`、OFF は `--switch-off`）。**ペア未設定なら共有行を出さない**。
- 主ボタン: 「追加する」／空なら「やることを入れると追加できます」。
- 保存は `insertMemoAction`（`features/memo-shortcut/actions.ts`、`memoFormSchema`: memo ≤30, isPair）。成功で閉じ、入力を空に。

### 予定シート（PlanAdd / PlanEdit）
- 内容高シート、`aria-label` はタイトル。gap 14。ヘッダー: × ｜ **「予定を追加」／「予定を編集」** + 共有なら「共有」タグ ｜ 編集時のみゴミ箱（`aria-label="この予定を削除"`）。
- 予定名: `--card` h52 px14 r12、`<input aria-label="予定名" placeholder="予定名（30文字まで）">` 18/600。
- 日付カード `--card` r14:
  - 単日: 行 h50「日付」16 + ボタン（h34 px12 r8 `--fill-soft` 15/600）「9/25（金）」。
  - 期間: 行「期間」16 + 日数「3日間」13 `--muted-foreground` + ボタン「9/21（月） 〜 9/23（水）」（`aria-label="期間を選ぶ"`）。
  - 区切り `margin-left 14`。「期間を指定」行 h50 + スイッチ（OFF は `--disabled`）。**ON にした瞬間 `end = max(end, start+1)`（最低 2 日間）**、OFF で単日。
  - 暦はボタンの直下に展開（D8。既存 react-day-picker を v2 トークンで塗る: 選択 `--primary`、日曜 `--destructive`、土曜 `--saturday`、r12）。
- カテゴリ: ラベル「カテゴリ」13 `--muted-foreground` pl4。チップ `flex-wrap` gap 8、h36 px12 r999 14/600 gap 6、先頭 10px ドット。選択: `color-mix(色 15%, var(--card))` 地・`border 1.5px 色`・文字は色。非選択: `--card` 地・border 透明・`--foreground`。「なし」: ドット `--dash`、選択時の文字は `--foreground`。
- メモ: ラベル「メモ」13。`<textarea aria-label="メモ" placeholder="任意。URL を貼るとリンクになります">` h84 `padding 12 14` r12 `--card` 15px。
- フッター `sticky bottom 0`、`margin 4 -16 0`、`padding 12 16 34`、`--background` 地、`border-top 1px var(--border)`: 主ボタン「追加する」／「保存する」、無効「予定名を入れると追加できます」／「予定名を入れると保存できます」。**削除ボタンはフッターに置かない**（ヘッダー右）。
- 削除の確認（ConfirmAlert）: 「この予定を削除しますか？」／「「{予定名}」を削除します。削除すると元に戻せません。」／やめる｜削除。
- Conform のフィールドエラーはデザインに無い。サーバ検証失敗はトーストで出す。

## 現状
- `src/v2/features/calendar/components/{calendar-screen,month-grid,todo-chips,day-detail-list}.tsx`、`src/v2/features/plan/components/plan-sheet.tsx`、`src/app/v2/(tabs)/calendar/page.tsx`。差分: ベル無し、今日太字・月外薄表示、罫線色、TODO は横スクロールで追加無し、日別行にシェブロン無し・半角括弧・「すべての記録」リンク（`/records` は壊れている）、予定シートは白地・「キャンセル」文字・削除がフッター・旧 ConfirmDialog・スイッチ寸法・期間 ON 時の終了日。

## やること
1. カレンダー画面の上記差分を全て潰す。`headerLeft` に T03 の `NotificationBell` を page から渡す。
2. TODO 帯の折り返し・件数・「＋ 追加」・TODO 追加シート（`v2/features/calendar/components/todo-sheet.tsx`）。
3. 日別カードの行構成（シェブロン・区切り・タイル）、リマインダー行 → お知らせシート、日付書式。
4. 予定シートの上記差分。`ConfirmAlert` へ差し替え。
5. `calendar/page.tsx` から `getShortCutList` の取得を外す（D7。T05 で `RecordSheet` が `shortcuts` を要らなくなるまでは props で空配列を渡す）。
6. `features/calendar/domain` / `v2/features/calendar/domain` のテストを更新。

## 完了条件
- デモ（ペアあり／なし）でカレンダーを開き、月移動・TODO 追加/削除・予定追加/編集/削除・帯の表示がデザインどおり。ペアなしでは TODO シートに共有行が出ない。

## 完了メモ
